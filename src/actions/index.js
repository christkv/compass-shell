const Reflux = require('reflux');

const MongoShellActions = Reflux.createActions([
  /**
   * define your actions as strings below, for example:
   */
  'execute', 'initialize'
]);

module.exports = MongoShellActions;
